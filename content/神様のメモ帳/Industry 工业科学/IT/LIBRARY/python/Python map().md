---
aliases:
  - map()
  - Python map()
tags:
  - flag/LANGUAGE/Python
  - Label/Industry-工业科学/IT/Library/BuiltinFunction
  - flag/Library/BusinessLogicLayer/Implement__/FP
---

```python
map01 = map(
    func_obj,
    iterable_obj,
)

list(map01)
# map() 只是构造了 map 对象，并未执行，迭代才会真正执行
# 论可读性，生成器表达式 优于 map()
# 论可读性，列表推导式 优于 list(map())
```

